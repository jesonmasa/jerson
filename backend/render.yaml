# render.yaml - Configuración para despliegue en Render
# Usa Dockerfile para tener Node.js + Python

services:
  - type: web
    name: constructor-backend
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000